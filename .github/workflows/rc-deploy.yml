name: rc-deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-22.04-arm
    steps:
      - name: checkout
        uses: actions/checkout@v3
      
      - name: Setup environment variable from webdav
        run: |
          curl -u ${{ secrets.CREDENTIAL_NAME }}:${{ secrets.CREDENTIAL_PW }} -X GET https://webdav.hwangdo.kr/docker-registry/rc-deploy.txt -o env.txt
          echo "DOCKER_USERNAME=$(sed -n '1p' env.txt)" >> $GITHUB_ENV
          echo "DOCKER_PASSWORD=$(sed -n '2p' env.txt)" >> $GITHUB_ENV
      
      - name: Log in to Docker registry
        run: |
          echo "$DOCKER_PASSWORD" | docker login docker.hwangdo.kr -u "$DOCKER_USERNAME" --password-stdin
          
      - name: Build docker image
        run: |
          docker build -t docker.hwangdo.kr/algohub-client-rc:latest .
      
      - name: Push docker image
        run: |
          docker push docker.hwangdo.kr/algohub-client-rc:latest